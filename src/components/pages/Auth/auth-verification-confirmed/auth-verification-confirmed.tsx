import { AuthVerificationConfirmed } from "@/components/layouts";

export const AuthVerificationConfirmedPage = () => {
  return (
    <main className="flex w-full min-h-screen">
      <AuthVerificationConfirmed />
    </main>
  );
};
