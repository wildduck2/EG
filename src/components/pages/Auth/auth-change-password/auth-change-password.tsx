import { AuthChangePassword, AuthSide } from "@/components/layouts";

export const AuthChangePasswordPage = () => {
  // <AuthSide />
  return (
    <main className="flex w-full min-h-screen">
      <AuthChangePassword />
    </main>
  );
};
